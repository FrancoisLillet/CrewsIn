<%= simple_form_for([@user, @enrollment], data: {turbo: false}) do |f| %>
        <%= f.collection_select :mate_id, @ur_mates, :id, :first_name %>
        <%= f.collection_select :trip_id, @ur_trips, :id, :country %>
        <%= f.submit "Submit", class: "btn btn-success"%>
    <% end %>
